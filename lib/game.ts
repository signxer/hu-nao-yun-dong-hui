export type GameMode = 'ai' | 'local' | 'online';
export type Locale = 'zh' | 'en';
export type BoardName = 'Mild Mile' | 'Wild Wilds';

export type RacerName =
  | 'Alchemist' | 'Baba Yaga' | 'Banana' | 'Blimp' | 'Cheerleader' | 'Centaur'
  | 'Coach' | 'Copy Cat' | 'Dicemonger' | 'Duelist' | 'Egg' | 'Flip Flop'
  | 'Genius' | 'Gunk' | 'Hare' | 'Heckler' | 'Huge Baby' | 'Hypnotist'
  | 'Inchworm' | 'Lackey' | 'Leaptoad' | 'Legs' | 'Lovable Loser' | 'M.O.U.T.H.'
  | 'Magician' | 'Mastermind' | 'Party Animal' | 'Rocket Scientist' | 'Romantic'
  | 'Scoocher' | 'Sisyphus' | 'Skipper' | 'Stickler' | 'Suckerfish' | 'Third Wheel'
  | 'Twin';

export interface RacerCard { name: RacerName; tagline: string; ability: string; zhAbility: string; color: string; token: number; speed: number; }
export interface RacerState { id: string; name: RacerName; ownerId: string; position: number; score: number; tripped: boolean; eliminated: boolean; finished: number | null; lastRoll: number | null; }
export interface PlayerState { id: string; name: string; color: string; isAi?: boolean; team: RacerName[]; score: number; }
export interface GameState { mode: GameMode; locale: Locale; phase: 'home' | 'draft' | 'race' | 'result' | 'game-over' | 'room'; board: BoardName; raceNumber: number; players: PlayerState[]; racers: RacerState[]; currentPlayer: number; finishers: string[]; log: string[]; roomCode?: string; connected?: number; }

const colors = ['#f20f2b', '#1a76ff', '#9c42ff', '#18a85b', '#ff9f0b', '#ef3c9b'];
const rawRacers: Array<[RacerName, string, string, string, number]> = [
  ['Alchemist', 'Forever turning This into That.', 'On a 1 or 2, move 4 instead.', '掷出 1 或 2 时，改为移动 4 格。', 4.7], ['Baba Yaga', 'This property is going fast.', 'If you share with Duelist, Duelist trips.', '与 Duelist 同格时，让 Duelist 跌倒。', 4.0], ['Banana', 'No more edible than you or I.', 'Trip any racer you pass.', '让被你经过的角色跌倒。', 4.1], ['Blimp', 'Built to fly, born to explode.', 'Before corner two, +3. After it, -1.', '第二个弯道前主移动 +3，之后 -1。', 5.7], ['Cheerleader', 'A being of pure pep.', 'If alone in last place, get +1.', '独自在最后一名时获得 +1。', 6.0], ['Centaur', 'Here’s the kicker.', 'Cannot be hoofwhacked farther back than Start.', '不会被踢回起点之后。', 4.1], ['Coach', 'Believe in yourself. Even as a lizard.', 'Everyone on your space gets +1.', '同格所有角色主移动 +1，包括自己。', 5.1], ['Copy Cat', 'Very similar to another, different cat.', 'Copy the power of the lead racer.', '复制领先角色的能力。', 6.0], ['Dicemonger', 'A career from a d100 table.', 'If someone rerolls, move 2 before they move.', '有人重掷时，先移动 2 格。', 7.1], ['Duelist', 'When you’ve got a rapier, everything looks like a duel.', 'Challenge a racer sharing your space.', '可与同格角色决斗。', 6.2], ['Egg', 'You should use the chicken.', 'Choose another racer’s power before the race.', '赛前选择另一个角色的能力。', 6.6], ['Flip Flop', 'Ever searching for its lost twin.', 'Warp to the same space as a racer behind you.', '传送到身后某个角色的格子。', 10.6], ['Genius', 'A fool in the ways of love.', 'Predict the Skipper; if right, take another turn.', '预测 Skipper；预测正确时再行动一次。', 3.9], ['Gunk', 'A bit of a hugger.', 'Reduce the move amount, not the die roll.', '减少移动距离，而不是骰子点数。', 4.0], ['Hare', 'The only thing he can’t outrun is hubris.', 'When second, move 2.', '处于第二名时移动 2 格。', 6.3], ['Heckler', 'Its constructive criticism.', 'Works for racers who are tripping.', '对跌倒的角色也会触发。', 7.2], ['Huge Baby', 'She’s 30.', 'Push racers behind you back to your space.', '把你身后的角色推回你的格子。', 4.1], ['Hypnotist', 'Implanted this false bio himself.', 'At start, warp to a space with a racer.', '回合开始时可传送到有角色的格子。', 4.2], ['Inchworm', 'Give her an inch and she’ll take an inch.', 'When another racer rolls 1, move 1.', '其他角色掷出 1 时移动 1 格。', 4.7], ['Lackey', 'If you’re happy, he’s happy.', 'Move before the other racer, if it matters.', '必要时在另一个角色之前移动。', 5.8], ['Leaptoad', 'Frequently seen from below.', 'Skip spaces occupied by other racers.', '跳过有其他角色的格子。', 5.3], ['Legs', 'Has arms, too, but who cares.', 'May skip rolling and move 5 instead.', '可以不掷骰，直接移动 5 格。', 2.0], ['Lovable Loser', 'Top of her class at Loser Academy.', 'At start, alone in last place gets +1.', '回合开始时独自在最后一名获得 +1。', 3.8], ['M.O.U.T.H.', 'Mastication Orifice Utilized Toward Hunger.', 'A lone racer sharing your space is eliminated.', '与恰好一个角色同格时，将其淘汰。', 4.0], ['Magician', 'He can also cast fireball.', 'Reroll your main move up to two times.', '主移动最多可重掷两次。', 4.8], ['Mastermind', 'The one scheme he can’t pull off is getting his parents back together.', 'Predict a racer; if right, finish second.', '预测赢家，正确时直接获得第二名。', 4.0], ['Party Animal', 'They put a hat on an animal and this happened.', 'Huge Baby parties onto your space: push back.', 'Huge Baby 进入你的格子时，把同格角色推回 1 格。', 5.1], ['Rocket Scientist', 'Moonlights as a brain surgeon.', 'Double the roll, then trip.', '可以将掷骰翻倍，但会跌倒。', 6.9], ['Romantic', 'The last of her species.', 'When exactly one racer shares a space, move 2.', '与恰好一个角色同格时移动 2 格。', 9.7], ['Scoocher', 'Ope, just gonna scooch by you there.', 'Move 1 whenever another power happens.', '每当其他能力触发时移动 1 格。', 10.7], ['Sisyphus', 'Stays humble, keeps grinding.', 'Sixes can send you back to Start for a point.', '掷出 6 时可回到起点换取分数。', 4.8], ['Skipper', 'The wheel is fake; he just goes forward.', 'After a 1, take the next turn.', '有人掷出 1 后，自己获得下一回合。', 3.8], ['Stickler', 'Proofread this rulebook without asking.', 'Other racers cannot overshoot the finish.', '其他角色必须精确移动到终点。', 4.1], ['Suckerfish', 'Has abandonment issues.', 'Ride with a racer until their full move ends.', '可跟随其他角色完成整段移动。', 8.3], ['Third Wheel', 'Searching for her throther half.', 'Warp to any space with exactly two racers.', '传送到恰好有两个角色的格子。', 5.0], ['Twin', 'Not related to anyone, just identical to everyone.', 'Copy a racer who won a previous race.', '复制上一场获胜角色的能力。', 7.8],
];
export const RACERS: RacerCard[] = rawRacers.map(([name, tagline, ability, zhAbility, speed], i) => ({ name, tagline, ability, zhAbility, speed, color: colors[i % colors.length], token: i }));
export const racerByName = (name: RacerName) => RACERS.find((r) => r.name === name)!;
export const wildEffects: Record<number, string> = { 1: 'star', 5: 'trip', 7: '+3', 11: '+1', 13: 'star', 16: '-4', 17: 'trip', 23: '+2', 24: '-2', 26: 'trip' };
export function createPlayers(mode: GameMode, count = 4): PlayerState[] { return Array.from({ length: count }, (_, i) => ({ id: `player-${i + 1}`, name: mode === 'ai' && i > 0 ? `AI ${i}` : `Player ${String.fromCharCode(65 + i)}`, color: colors[i], isAi: mode === 'ai' && i > 0, team: [], score: 0 })); }
export function makeRoomCode() { const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'; return Array.from({ length: 6 }, () => chars[Math.floor(Math.random() * chars.length)]).join(''); }
export function createGame(mode: GameMode, locale: Locale): GameState { return { mode, locale, phase: mode === 'online' ? 'room' : 'draft', board: 'Mild Mile', raceNumber: 1, players: createPlayers(mode), racers: [], currentPlayer: 0, finishers: [], log: [], roomCode: mode === 'online' ? makeRoomCode() : undefined, connected: mode === 'online' ? 1 : undefined }; }
export function chooseTeam(state: GameState, playerId: string, names: RacerName[]) { const p = state.players.find((player) => player.id === playerId); if (p) p.team = names; }
export function beginRace(state: GameState): GameState { const racers = state.players.flatMap((p) => { const picks = p.team.length ? p.team.slice(0, state.players.length === 2 ? 2 : 1) : [RACERS[Math.floor(Math.random() * RACERS.length)].name]; return picks.map((name, i) => ({ id: `${p.id}-${i}`, name, ownerId: p.id, position: 0, score: 0, tripped: false, eliminated: false, finished: null, lastRoll: null })); }); return { ...state, phase: 'race', racers, finishers: [], currentPlayer: 0, board: state.raceNumber % 2 ? 'Mild Mile' : 'Wild Wilds', log: [`Race ${state.raceNumber} · ${state.raceNumber % 2 ? 'Mild Mile' : 'Wild Wilds'}`, 'Everyone reveals a racer. Player A rolls first.'] }; }
function activeRacers(state: GameState) { return state.racers.filter((r) => !r.eliminated && r.finished === null); }
export function takeTurn(state: GameState, racerId?: string): GameState { if (state.phase !== 'race') return state; const actor = racerId ?? state.players[state.currentPlayer]?.id; const racer = state.racers.find((r) => r.ownerId === actor && !r.eliminated && r.finished === null); if (!racer) return { ...state, currentPlayer: (state.currentPlayer + 1) % state.players.length }; const card = racerByName(racer.name); const roll = Math.floor(Math.random() * 6) + 1; racer.lastRoll = roll; let distance = roll; const log: string[] = []; if (racer.name === 'Alchemist' && roll <= 2) distance = 4; if (racer.name === 'Blimp') distance += state.board === 'Mild Mile' ? 1 : -1; if (racer.name === 'Legs' && roll <= 3) distance = 5; if (racer.name === 'Rocket Scientist' && roll >= 4) { distance *= 2; racer.tripped = true; } if (racer.name === 'Gunk') distance = Math.max(0, distance - 1); const start = racer.position; const path = Array.from({ length: Math.max(0, distance) }, (_, i) => start + i + 1); const passed = state.racers.filter((other) => other.id !== racer.id && path.includes(other.position) && !other.eliminated && other.finished === null); racer.position = Math.min(30, Math.max(0, start + distance)); log.push(`${card.name} rolled ${roll}${distance !== roll ? ` → ${distance}` : ''} and moved ${start} → ${racer.position}.`); if (state.board === 'Wild Wilds') { const effect = wildEffects[racer.position]; if (effect === 'trip') { racer.tripped = true; log.push(`${card.name} landed on TRIP.`); } if (effect === 'star') { racer.score += 1; log.push(`${card.name} found a star (+1 point).`); } if (effect?.startsWith('+')) { racer.position = Math.min(30, racer.position + Number(effect)); log.push(`Arrow carried ${card.name} forward ${effect}.`); } if (effect?.startsWith('-')) { racer.position = Math.max(0, racer.position + Number(effect)); log.push(`Arrow sent ${card.name} backward ${effect}.`); } } if (racer.name === 'Banana') passed.forEach((other) => { other.tripped = true; log.push(`${other.name} was tripped while being passed.`); }); const same = activeRacers(state).filter((other) => other.position === racer.position && other.id !== racer.id); if (racer.name === 'Romantic' && same.length === 1) { racer.position = Math.min(30, racer.position + 2); log.push('Romantic moved 2 for love.'); } if (racer.name === 'M.O.U.T.H.' && same.length === 1) { same[0].eliminated = true; log.push(`${same[0].name} was CHOMPED.`); } if (racer.name === 'Scoocher') { racer.position = Math.min(30, racer.position + 1); log.push('Scoocher scooched 1.'); } if (racer.position >= 30) { racer.finished = state.finishers.length + 1; state.finishers.push(racer.id); const placementPoints = racer.finished === 1 ? 4 : racer.finished === 2 ? 2 : 0; state.players.find((p) => p.id === racer.ownerId)!.score += placementPoints + racer.score; log.push(`${card.name} crossed the finish line in ${racer.finished === 1 ? '1st' : racer.finished === 2 ? '2nd' : `${racer.finished}th`} place.`); } const next = { ...state, currentPlayer: (state.currentPlayer + 1) % state.players.length, log: [...state.log, ...log].slice(-12) }; if (state.finishers.length >= 2 || activeRacers(state).length <= 1) return { ...next, phase: 'result' }; return next; }
export function nextRace(state: GameState): GameState { if (state.raceNumber >= 4) return { ...state, phase: 'game-over' }; return beginRace({ ...state, raceNumber: state.raceNumber + 1 }); }
